# Shell
A simple shell in C

# Usage
Compile using
```bash
gcc -g -Wall -I . Shell.c String.c Command.c Pipe.c Array.c Console.c Process.c File.c -o Shell
```
To run test script
```bash
./Shell < shell-input
```
